{
  "name": "departure-shop",
  "version": "2.0.0",
  "private": true,
  "description": "Departure Clothing — full-stack storefront demo (root scripts for one-service deploys)",
  "engines": { "node": "22.x" },
  "scripts": {
    "build": "npm --prefix backend ci --omit=dev && npm --prefix frontend ci --include=dev && npm --prefix frontend run build",
    "start": "node backend/src/server.js"
  }
}
