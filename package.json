{
  "name": "boarding-pass-shop",
  "version": "2.0.0",
  "private": true,
  "description": "Boarding Pass Clothing — full-stack storefront demo (root scripts for one-service deploys)",
  "engines": { "node": "20.x" },
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "node backend/src/server.js"
  }
}
